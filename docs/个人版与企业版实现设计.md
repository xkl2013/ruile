# 睿乐大脑个人版与企业版订阅管理设计

文档日期：2026-09-14
适用仓库：`/Users/jamgogh/Desktop/agent/ruile`
状态：设计文档草案，先用于产品、后端、前端、后台和测试评审
范围：只设计订阅管理，不实施代码；覆盖个人版、企业版、存储配额、积分额度、用量账本和后台管理

参考实现：`DEEIX-AI/DEEIX-Chat`，本地参考 checkout 为 `/tmp/DEEIX-Chat`，`dev` 分支 commit `4e71bebe385d2b1d386924ed7232a35e10f9106e`。本文只借鉴其账单模型和并发结算思路，不照搬其用户级计费归属。

## 1. 设计结论

Ruile 的订阅管理采用“工作区订阅主体 + 账号行为审计”的模型：

```text
用户 User
  ├─ 拥有一个个人工作区 Tenant(personal)
  └─ 可以加入多个企业工作区 Tenant(enterprise)

订阅主体 Billing Subject
  └─ tenant_id

套餐 Plan
  ├─ edition_code: personal | enterprise
  ├─ 存储额度: storage_quota_bytes
  ├─ 周期积分: period_credit_points
  └─ 产品能力: feature_json / limits_json

用量 Usage
  ├─ personal_usage: 个人订阅额度和个人充值余额
  ├─ enterprise_allocated_usage: 企业分配给成员的额度
  └─ 每次付费调用按 active tenant 解析用量口径
```

核心原则：

1. `tenant` 仍是资源归属、RBAC、存储和审计边界。
2. 套餐订阅必须绑定 `tenant_id`，而不是只绑定 `user_id`。
3. 个人版就是个人工作区的订阅；企业版就是企业工作区的订阅。
4. 用量分成 `personal_usage` 和 `enterprise_allocated_usage`：个人工作区消耗个人用量，企业工作区只能消耗企业分配给成员的用量；账本必须记录真实 `actor_user_id`。
5. 存储配额继续以工作区为准，优先复用 `tenants.storage_quota` / `storage_used`。
6. 积分不是权限。权限仍由工作区成员、角色、知识库创建者、共享空间和 API Key 策略决定。
7. 前端只显示后端返回的套餐、能力、配额和状态；所有限制必须由后端再次执行。
8. “套餐订阅”和“知识库订阅”必须分开：套餐订阅是付费权益，知识库订阅只是快捷入口，不授予访问权。

## 2. DEEIX-Chat 可借鉴点

DEEIX-Chat 当前实现已经具备完整的用户级账单体系：

| 模块 | DEEIX-Chat 实现 | Ruile 借鉴方式 |
| --- | --- | --- |
| 套餐 | `BillingPlan` 保存 `Code`、`FeatureJSON`、`PeriodCreditNanousd`、`PermissionGroupID` | 保留“套餐定义 + 能力 JSON + 周期额度”的结构，但额度单位改为积分 |
| 价格 | `BillingPrice` 将套餐和 `month/year/lifetime`、币种、金额分离 | 保留套餐和价格分离，便于月付、年付、试用、手工开通 |
| 订阅 | `Subscription` 绑定 `UserID`、`PlanID`、周期起止、取消和自动续费状态 | 改为绑定 `TenantID`，同时记录订阅操作人 |
| 余额 | `BillingAccount` + `BalanceTransaction` 维护用户余额和余额流水 | 改为工作区积分账户和积分流水 |
| 用量预留 | `UsageReservation` 在模型调用前预留预算，限制并发透支 | 借鉴预留机制，按工作区和用量口径锁定预算 |
| 用量账本 | `UsageLedger` 记录模型、Token、调用次数、耗时、费用和快照 | 扩展为工作区账本，保留调用用户、知识库、Agent、渠道等上下文 |
| 支付配置 | `system_settings` 中维护 `billing.payment_providers`、Stripe、EPay 网关、商户号和密钥 | Ruile 复用 SystemAdmin 运行时配置思路，增加支付宝和微信支付配置 |
| 支付收银台 | `/billing/payments/checkout` 先创建内部订单，再创建第三方收银台并保存 `checkout_url` | Ruile 保持“订单先落库，第三方收银台后创建”，但订单绑定 `tenant_id` |
| 支付回调 | Stripe webhook / EPay notify 校验签名、金额、币种和订单号后调用 `CompletePaymentOrder` | Ruile 为支付宝、微信支付增加公开 notify 路由，回调幂等发放积分或存储 |
| 存储配额 | `file_storage_quotas` 以用户为维度记录 `quota/used/reserved`，上传事务内扣占用 | Ruile 已有工作区级 `storage_quota/storage_used`，补齐预留和流水即可 |
| 接口 | 用户侧 `/billing/*`，后台 `/admin/billing/*` | Ruile 增加用户侧、企业管理员侧和 SystemAdmin 侧三层接口 |

不能直接照搬的地方：

- DEEIX-Chat 是 `billing_accounts.user_id`、`billing_subscriptions.user_id`、`UsageLedger.user_id` 的用户级计费；Ruile 必须按 `tenant_id` 作为订阅和扣费主体。
- DEEIX-Chat 的 `PermissionGroup` 是模型访问和费率倍率，不等同于 Ruile 的企业成员角色。
- DEEIX-Chat 的文件存储配额是用户维度；Ruile 的知识库文件、索引和默认存储实例已经是工作区维度。
- DEEIX-Chat 的 `FeatureJSON` 可用于套餐展示，但 Ruile 的版本/套餐能力必须在后端服务和中间件中执行。
- DEEIX-Chat 的 EPay 是支付宝/微信聚合支付入口；Ruile 面向国内部署时建议把 `alipay` 和 `wechat_pay` 做成一等支付渠道，EPay 仅作为可选兼容通道。

## 3. Ruile 现有基础

当前 Ruile 可复用的对象：

| 现有对象 | 作用 |
| --- | --- |
| `users` | 账号身份，不拆个人账号和企业账号 |
| `tenants` | 工作区和资源归属边界，已有 `storage_quota`、`storage_used`、`default_storage_backend_id` |
| `tenant_members` | 企业成员、角色、状态，已有 `owner/admin/contributor/viewer` |
| `knowledge_bases.tenant_id` | 知识库归属工作区 |
| `knowledges.storage_size` | 单条知识的存储占用，用于回收和统计 |
| `storage_backends` | 工作区级存储实例 |
| `organizations` / `organization_tenant_members` / `kb_shares` | 共享空间与知识库共享授权 |
| `knowledge_base_subscriptions` | 知识库快捷订阅，不是付费订阅 |
| `audit_logs` | 工作区和系统审计 |
| `/api/v1/system/admin/*` | 平台运维管理入口 |

当前已经出现的 `types.EditionCode` / `EditionEntitlementsForTenant` 可以作为产品版本能力的起点，但本次订阅设计需要再增加“套餐、价格、周期、积分、支付和账本”的独立模型。不要把 `EditionEntitlements` 当成完整订阅系统。

## 4. 产品版本与套餐

### 4.1 版本定义

只对外展示两个产品版本：

| 版本 | 订阅主体 | 适用对象 | 核心边界 |
| --- | --- | --- | --- |
| 个人版 `personal` | 个人工作区 `tenant_id` | 单账号个人使用 | 无成员管理、无企业共享、无企业发布渠道 |
| 企业版 `enterprise` | 企业工作区 `tenant_id` | 多人协作组织 | 成员、角色、共享空间、企业知识库、渠道和审计 |

兼容状态：

- `legacy` 只用于历史工作区迁移，不作为销售版本。
- `space_type=personal` 可映射为个人版。
- `space_type=organization` 可映射为企业版。
- 无法判定的旧工作区先按兼容策略运行，由 SystemAdmin 分类。

### 4.2 套餐建议

V1 建议四个套餐，具体额度可由运营配置：

| 套餐 code | 版本 | 成员 | 存储 | 周期积分 | 适合场景 |
| --- | --- | ---: | ---: | ---: | --- |
| `personal_free` | 个人版 | 1 | 1 GB | 试用额度 | 注册体验 |
| `personal_pro` | 个人版 | 1 | 20 GB | 中等额度 | 个人长期使用 |
| `enterprise_team` | 企业版 | 10 | 100 GB | 团队额度 | 小团队/园所 |
| `enterprise_business` | 企业版 | 可配置 | 可配置 | 可配置 | 大客户合同 |

说明：

- 免费套餐也走订阅模型，便于统一返回当前权益。
- 企业版成员数应使用 `tenant_members` 活跃成员计数校验。
- 存储是硬额度，默认不自动用积分购买。
- 积分是 AI 计算额度，用于模型、OCR/ASR、重排、摘要、知识图谱、自动问题生成、Agent 和发布渠道调用。
- 企业版周期积分先进入企业额度池，再通过成员分配形成 `enterprise_allocated_usage` 后才可消费。

### 4.3 能力键

继续保留版本能力键，但由订阅套餐生成最终能力：

```text
workspace.members.manage
workspace.shared_spaces
workspace.enterprise_invite
knowledge.share
knowledge.subscribe
knowledge.publish
agent.enterprise_manage
skill.enterprise_manage
channel.embed
channel.im
tenant.api_keys
tenant.audit_log
tenant.storage_backend_manage
knowledge.defaults_manage
billing.enterprise_allocation_manage
billing.payment_online
billing.topup
billing.storage_addon_purchase
billing.usage_export
```

能力解析规则：

```text
base edition policy
  + active subscription plan feature_json
  + SystemAdmin configured tenant override
  - expired / suspended / past_due restrictions
  = effective entitlements
```

禁止规则：

- 个人版即使套餐 JSON 打开了 `workspace.members.manage`，后端也必须拒绝。
- 企业版即使用户是 Owner，也不能访问员工个人工作区资产。
- SystemAdmin 运营后台可以调整套餐和订阅，但普通企业管理员不能自行修改套餐能力 JSON。

## 5. 存储设计

### 5.1 配额口径

Ruile 继续以工作区统计存储：

```text
tenants.storage_quota  工作区总存储额度，单位 bytes
tenants.storage_used   工作区已使用额度，单位 bytes
```

计入口径：

- 知识库原始文件；
- 解析后保存在对象存储中的图片、音频、视频、导出文件；
- 检索索引的估算体积；
- FAQ、手工知识、网页导入产生的向量索引估算体积；
- 后续若聊天附件长期保存，也应进入当前 active tenant 的存储占用。

暂不计入口径：

- 日志、审计、数据库普通业务行；
- Redis 队列和临时缓存；
- 可自动清理的临时上传缓存；
- 外部模型供应商侧缓存。

### 5.2 套餐与存储关系

套餐激活时：

1. 读取套餐 `storage_quota_bytes`。
2. 如果新额度小于当前 `storage_used`，订阅仍可变更，但进入 `storage_over_quota` 状态。
3. 超额状态下允许读取、搜索、删除、导出，禁止新增上传、复制、重解析产生新索引。
4. 企业升级或购买存储包后，更新 `tenants.storage_quota` 并记录审计。

降级策略：

- 不删除文件。
- 不自动压缩或迁移存储。
- 不允许继续新增占用。
- 前端显示“已超出当前套餐存储额度”，引导升级或清理。

### 5.3 存储预留

当前上传只在部分链路判断 `storage_used >= storage_quota`，容易在并发上传和解析后写入索引时超出。建议增加工作区级预留：

```sql
CREATE TABLE tenant_storage_reservations (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    tenant_id BIGINT NOT NULL,
    actor_user_id VARCHAR(36) NOT NULL,
    ref_no VARCHAR(128) NOT NULL,
    reserved_bytes BIGINT NOT NULL DEFAULT 0,
    status VARCHAR(32) NOT NULL DEFAULT 'active',
    expires_at TIMESTAMP WITH TIME ZONE NOT NULL,
    settled_at TIMESTAMP WITH TIME ZONE,
    released_at TIMESTAMP WITH TIME ZONE,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX uq_tenant_storage_reservations_ref
    ON tenant_storage_reservations (tenant_id, ref_no);

CREATE INDEX idx_tenant_storage_reservations_active
    ON tenant_storage_reservations (tenant_id, status, expires_at);
```

上传/解析流程：

```text
上传前估算原文件大小
  -> 锁定 tenant 行
  -> 校验 storage_used + active_reserved + new_bytes <= storage_quota
  -> 创建 storage reservation
  -> 文件写入对象存储
  -> 创建 knowledge/resource 记录
  -> 解析、切片、向量化后得到实际 storage_size
  -> tenant.storage_used += actual_storage_size
  -> reservation settled/released
```

如果解析失败：

- 已持久化的原始文件按实际占用结算；
- 未产生的索引预留释放；
- 失败记录仍显示占用，删除失败知识时释放。

### 5.4 存储流水

为审计和问题排查增加存储流水：

```sql
CREATE TABLE tenant_storage_transactions (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    tenant_id BIGINT NOT NULL,
    actor_user_id VARCHAR(36),
    type VARCHAR(32) NOT NULL,
    amount_bytes BIGINT NOT NULL,
    storage_used_after BIGINT NOT NULL,
    ref_type VARCHAR(64) NOT NULL DEFAULT '',
    ref_id VARCHAR(64) NOT NULL DEFAULT '',
    ref_no VARCHAR(128) NOT NULL DEFAULT '',
    description VARCHAR(255) NOT NULL DEFAULT '',
    snapshot_json JSONB,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_tenant_storage_transactions_tenant_created
    ON tenant_storage_transactions (tenant_id, created_at DESC);
```

流水类型：

```text
knowledge_upload
knowledge_index
faq_import
manual_knowledge
delete_release
quota_set
subscription_apply
storage_addon
reconcile_adjust
```

### 5.5 存储包发放记录

支付购买存储包后，不能只更新 `tenants.storage_quota`。需要记录每次购买发放，便于幂等、审计、退款人工核对和后续套餐降级计算。

```sql
CREATE TABLE tenant_storage_addon_grants (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    tenant_id BIGINT NOT NULL,
    order_no VARCHAR(64) NOT NULL UNIQUE,
    actor_user_id VARCHAR(36) NOT NULL,
    storage_quota_bytes BIGINT NOT NULL,
    effective_at TIMESTAMP WITH TIME ZONE NOT NULL,
    expires_at TIMESTAMP WITH TIME ZONE,
    status VARCHAR(32) NOT NULL DEFAULT 'active',
    snapshot_json JSONB NOT NULL DEFAULT '{}',
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_tenant_storage_addon_grants_tenant
    ON tenant_storage_addon_grants (tenant_id, status, expires_at);
```

规则：

- `order_no` 唯一，支付平台重复通知时不会重复加存储。
- V1 建议存储包长期有效；若运营设置 `expires_at`，到期只影响新增写入，不自动删除历史文件。
- 工作区总存储额度 = 当前套餐额度 + 有效存储包额度 + SystemAdmin 手工调额。

## 6. 积分设计

### 6.1 积分单位

对用户展示整数积分，数据库用微积分避免小数误差：

```text
1 积分 = 1,000,000 point_micros
```

所有扣费、赠送、充值和周期额度字段使用 `BIGINT` 存 `point_micros`。

### 6.2 积分来源与用量口径

积分来源分两类：

| 类型 | 字段 | 生命周期 | 用途 |
| --- | --- | --- | --- |
| 周期积分 | `tenant_subscriptions.period_credit_point_micros` | 当前订阅周期内有效，到期重置 | 套餐包含额度 |
| 余额积分 | `tenant_credit_accounts.balance_point_micros` | 长期有效，除非运营设置过期 | 充值、赠送、超额扣费 |

用量对用户展示和授权时分两类：

| 用量口径 | 工作区 | 来源 | 消耗人 | 管理人 |
| --- | --- | --- | --- | --- |
| `personal_usage` | 个人工作区 | 个人版周期积分、个人充值余额 | 个人工作区 Owner | 本人 |
| `enterprise_allocated_usage` | 企业工作区 | 企业版周期积分、企业充值余额中分配给成员的额度 | 企业成员本人 | 企业 Owner/Admin |

个人版：

- 个人工作区内的付费调用只消耗 `personal_usage`。
- 加入企业后，在企业工作区内的调用不消耗个人用量。
- 个人充值余额不能自动转入企业，也不能被企业管理员分配。

企业版：

- 企业订阅和充值形成企业可用额度池，但成员不能无限共享。
- Owner/Admin 必须为成员配置默认或单独的 `enterprise_allocated_usage`。
- 企业成员在企业工作区内的付费调用只消耗分配给自己的企业用量。
- 账本按 `actor_user_id` 和 `allocation_id` 记录实际使用人和分配来源。

消耗顺序：

```text
personal_usage:
  个人周期积分 -> 个人余额积分 -> 拒绝付费调用

enterprise_allocated_usage:
  成员已分配周期额度 -> 成员已分配企业余额额度 -> 拒绝付费调用或申请增配
```

### 6.3 积分账户

```sql
CREATE TABLE tenant_credit_accounts (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    tenant_id BIGINT NOT NULL UNIQUE,
    balance_point_micros BIGINT NOT NULL DEFAULT 0,
    status VARCHAR(32) NOT NULL DEFAULT 'active',
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

### 6.4 积分流水

```sql
CREATE TABLE tenant_credit_transactions (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    account_id VARCHAR(36) NOT NULL,
    tenant_id BIGINT NOT NULL,
    actor_user_id VARCHAR(36),
    usage_scope VARCHAR(32) NOT NULL DEFAULT '',
    allocation_id VARCHAR(36) NOT NULL DEFAULT '',
    type VARCHAR(32) NOT NULL,
    amount_point_micros BIGINT NOT NULL,
    balance_after_point_micros BIGINT NOT NULL,
    ref_type VARCHAR(64) NOT NULL DEFAULT '',
    ref_id VARCHAR(64) NOT NULL DEFAULT '',
    ref_no VARCHAR(128) NOT NULL DEFAULT '',
    description VARCHAR(255) NOT NULL DEFAULT '',
    snapshot_json JSONB,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX uq_tenant_credit_transactions_ref
    ON tenant_credit_transactions (tenant_id, type, ref_no)
    WHERE ref_no <> '';

CREATE INDEX idx_tenant_credit_transactions_tenant_created
    ON tenant_credit_transactions (tenant_id, created_at DESC);
```

流水类型：

```text
subscription_grant
topup
redemption
usage_debit
usage_refund
admin_adjust
expire
reconcile_adjust
```

### 6.5 用量定价

新增统一定价表，先覆盖模型和内部服务：

```sql
CREATE TABLE billing_model_prices (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    model_key VARCHAR(128) NOT NULL UNIQUE,
    provider VARCHAR(64) NOT NULL DEFAULT '',
    pricing_mode VARCHAR(32) NOT NULL DEFAULT 'token',
    input_point_micros_per_m_tokens BIGINT NOT NULL DEFAULT 0,
    output_point_micros_per_m_tokens BIGINT NOT NULL DEFAULT 0,
    cache_read_point_micros_per_m_tokens BIGINT NOT NULL DEFAULT 0,
    cache_write_point_micros_per_m_tokens BIGINT NOT NULL DEFAULT 0,
    call_point_micros_per_call BIGINT NOT NULL DEFAULT 0,
    duration_point_micros_per_second BIGINT NOT NULL DEFAULT 0,
    status VARCHAR(32) NOT NULL DEFAULT 'active',
    snapshot_json JSONB,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE billing_service_prices (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    service_code VARCHAR(64) NOT NULL UNIQUE,
    service_name VARCHAR(128) NOT NULL DEFAULT '',
    pricing_mode VARCHAR(32) NOT NULL DEFAULT 'call',
    point_micros_per_call BIGINT NOT NULL DEFAULT 0,
    point_micros_per_unit BIGINT NOT NULL DEFAULT 0,
    unit_name VARCHAR(32) NOT NULL DEFAULT '',
    status VARCHAR(32) NOT NULL DEFAULT 'active',
    snapshot_json JSONB,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

第一批服务码：

```text
chat.completion
agent.run
knowledge.embedding
knowledge.summary
knowledge.question_generation
knowledge.graph_extract
file.ocr
file.asr
retrieval.rerank
web_search.query
mcp.tool_call
embed.chat
im.chat
```

### 6.6 用量预留

借鉴 DEEIX-Chat 的 `UsageReservation`，在上游调用前预留预算，避免并发透支：

```sql
CREATE TABLE tenant_usage_reservations (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    tenant_id BIGINT NOT NULL,
    actor_user_id VARCHAR(36) NOT NULL,
    usage_scope VARCHAR(32) NOT NULL,
    allocation_id VARCHAR(36) NOT NULL DEFAULT '',
    ref_no VARCHAR(128) NOT NULL,
    mode VARCHAR(32) NOT NULL DEFAULT 'subscription',
    reserved_period_point_micros BIGINT NOT NULL DEFAULT 0,
    reserved_balance_point_micros BIGINT NOT NULL DEFAULT 0,
    period_limit_point_micros BIGINT NOT NULL DEFAULT 0,
    period_start_at TIMESTAMP WITH TIME ZONE,
    period_end_at TIMESTAMP WITH TIME ZONE,
    status VARCHAR(32) NOT NULL DEFAULT 'active',
    usage_ledger_id VARCHAR(36),
    expires_at TIMESTAMP WITH TIME ZONE NOT NULL,
    settled_at TIMESTAMP WITH TIME ZONE,
    released_at TIMESTAMP WITH TIME ZONE,
    reconciliation_at TIMESTAMP WITH TIME ZONE,
    failure_code VARCHAR(64) NOT NULL DEFAULT '',
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX uq_tenant_usage_reservations_ref
    ON tenant_usage_reservations (tenant_id, ref_no);

CREATE INDEX idx_tenant_usage_reservations_active
    ON tenant_usage_reservations (tenant_id, status, expires_at);
```

预留规则：

1. 为每次模型/服务调用生成 `ref_no`。
2. 根据模型、工具、输入规模预估积分。
3. 按 active tenant 解析 `usage_scope`。
4. 企业工作区必须解析到成员有效 `allocation_id`。
5. 同一事务锁定订阅主体的积分账户；企业分配用量还要锁定成员分配记录。
6. 先占用对应口径的周期额度，再占用对应口径的余额额度。
7. 如果该口径额度不足，拒绝请求。
8. 调用成功后按真实用量结算。
9. 调用未产生上游费用时释放预留。
10. 上游已产生费用但结算失败时进入 `reconciliation`。

### 6.7 用量账本

```sql
CREATE TABLE tenant_usage_ledgers (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    tenant_id BIGINT NOT NULL,
    actor_user_id VARCHAR(36) NOT NULL,
    usage_scope VARCHAR(32) NOT NULL,
    allocation_id VARCHAR(36) NOT NULL DEFAULT '',
    ref_no VARCHAR(128) NOT NULL DEFAULT '',
    source VARCHAR(32) NOT NULL DEFAULT 'web',
    session_id VARCHAR(36) NOT NULL DEFAULT '',
    agent_id VARCHAR(36) NOT NULL DEFAULT '',
    knowledge_base_id VARCHAR(36) NOT NULL DEFAULT '',
    knowledge_id VARCHAR(36) NOT NULL DEFAULT '',
    service_code VARCHAR(64) NOT NULL DEFAULT '',
    model_key VARCHAR(128) NOT NULL DEFAULT '',
    provider VARCHAR(64) NOT NULL DEFAULT '',
    input_tokens BIGINT NOT NULL DEFAULT 0,
    output_tokens BIGINT NOT NULL DEFAULT 0,
    reasoning_tokens BIGINT NOT NULL DEFAULT 0,
    call_count BIGINT NOT NULL DEFAULT 0,
    duration_seconds BIGINT NOT NULL DEFAULT 0,
    latency_ms BIGINT NOT NULL DEFAULT 0,
    billed_point_micros BIGINT NOT NULL DEFAULT 0,
    period_covered_point_micros BIGINT NOT NULL DEFAULT 0,
    balance_charged_point_micros BIGINT NOT NULL DEFAULT 0,
    balance_after_point_micros BIGINT,
    billing_at TIMESTAMP WITH TIME ZONE NOT NULL,
    usage_date DATE NOT NULL,
    pricing_snapshot_json JSONB,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX uq_tenant_usage_ledgers_ref
    ON tenant_usage_ledgers (tenant_id, ref_no)
    WHERE ref_no <> '';

CREATE INDEX idx_tenant_usage_ledgers_tenant_date
    ON tenant_usage_ledgers (tenant_id, usage_date DESC);

CREATE INDEX idx_tenant_usage_ledgers_actor_date
    ON tenant_usage_ledgers (tenant_id, actor_user_id, usage_date DESC);

CREATE INDEX idx_tenant_usage_ledgers_allocation_date
    ON tenant_usage_ledgers (tenant_id, allocation_id, usage_date DESC)
    WHERE allocation_id <> '';
```

账本快照必须记录：

- 订阅 ID、套餐 code、周期起止；
- 本次使用的模型、服务、单价；
- 周期积分覆盖多少；
- 余额积分扣了多少；
- 调用入口：Web、API、IM、Embed、后台任务；
- 资源上下文：知识库、知识条目、Agent、会话；
- 失败/待核对原因。

## 7. 订阅数据模型

### 7.1 套餐表

```sql
CREATE TABLE billing_plans (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    code VARCHAR(64) NOT NULL UNIQUE,
    edition_code VARCHAR(32) NOT NULL,
    name VARCHAR(128) NOT NULL,
    description VARCHAR(512) NOT NULL DEFAULT '',
    status VARCHAR(32) NOT NULL DEFAULT 'active',
    sort_order INT NOT NULL DEFAULT 0,
    storage_quota_bytes BIGINT NOT NULL DEFAULT 0,
    period_credit_point_micros BIGINT NOT NULL DEFAULT 0,
    feature_json JSONB NOT NULL DEFAULT '{}',
    limits_json JSONB NOT NULL DEFAULT '{}',
    metadata_json JSONB NOT NULL DEFAULT '{}',
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_billing_plans_edition_status
    ON billing_plans (edition_code, status, sort_order);
```

`limits_json` 示例：

```json
{
  "tenant.max_members": 10,
  "tenant.max_knowledge_bases": 50,
  "tenant.max_agents": 20,
  "tenant.max_shared_spaces": 5,
  "tenant.max_api_keys": 3,
  "billing.default_member_monthly_allocation_points": 10000
}
```

### 7.2 价格表

```sql
CREATE TABLE billing_prices (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    plan_id VARCHAR(36) NOT NULL REFERENCES billing_plans(id),
    code VARCHAR(64) NOT NULL UNIQUE,
    billing_interval VARCHAR(16) NOT NULL DEFAULT 'month',
    currency VARCHAR(16) NOT NULL DEFAULT 'CNY',
    amount_cents BIGINT NOT NULL DEFAULT 0,
    is_default BOOLEAN NOT NULL DEFAULT FALSE,
    status VARCHAR(32) NOT NULL DEFAULT 'active',
    external_price_ref VARCHAR(128) NOT NULL DEFAULT '',
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_billing_prices_plan_status
    ON billing_prices (plan_id, status, is_default);
```

支持周期：

```text
month
year
lifetime
trial
manual_contract
```

### 7.3 工作区订阅表

```sql
CREATE TABLE tenant_subscriptions (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    tenant_id BIGINT NOT NULL,
    plan_id VARCHAR(36) NOT NULL REFERENCES billing_plans(id),
    price_id VARCHAR(36) REFERENCES billing_prices(id),
    status VARCHAR(32) NOT NULL DEFAULT 'active',
    start_at TIMESTAMP WITH TIME ZONE NOT NULL,
    current_period_start_at TIMESTAMP WITH TIME ZONE NOT NULL,
    current_period_end_at TIMESTAMP WITH TIME ZONE,
    cancel_at_period_end BOOLEAN NOT NULL DEFAULT FALSE,
    canceled_at TIMESTAMP WITH TIME ZONE,
    auto_renew BOOLEAN NOT NULL DEFAULT FALSE,
    subscribed_by_user_id VARCHAR(36) NOT NULL DEFAULT '',
    payment_provider VARCHAR(32) NOT NULL DEFAULT '',
    external_subscription_ref VARCHAR(128) NOT NULL DEFAULT '',
    snapshot_json JSONB NOT NULL DEFAULT '{}',
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_tenant_subscriptions_tenant_status
    ON tenant_subscriptions (tenant_id, status, current_period_end_at);

CREATE INDEX idx_tenant_subscriptions_plan
    ON tenant_subscriptions (plan_id);
```

订阅状态：

```text
active      生效
trialing    试用
past_due    待续费，限制新增消耗
canceled    已取消
expired     已过期
suspended   运营手动暂停
```

同一 `tenant_id` 同一时间只能有一个当前有效订阅。历史订阅保留用于审计和对账。

### 7.4 支付渠道设置

参考 DEEIX-Chat 的 `billing.payment_providers`、`epay_gateway_url`、`epay_types` 设计，Ruile 支付配置放在现有 `system_settings`，由 SystemAdmin 管理。生产环境优先使用支付宝、微信支付原生通道；需要快速接入聚合支付时，可保留 EPay 兼容通道。

推荐配置键：

| key | 类型 | 密钥 | 说明 |
| --- | --- | --- | --- |
| `billing.payment_providers` | string_list | 否 | 启用渠道：`alipay`、`wechat_pay`、`epay` |
| `billing.public_api_base_url` | string | 否 | 拼接异步通知地址，生产必须是 HTTPS |
| `billing.public_web_base_url` | string | 否 | 拼接支付成功/取消后的前端返回地址 |
| `billing.display_currency` | string | 否 | V1 固定 `CNY` |
| `billing.alipay_gateway_url` | string | 否 | 默认 `https://openapi.alipay.com/gateway.do` |
| `billing.alipay_app_id` | string | 否 | 支付宝应用 ID |
| `billing.alipay_private_key` | string | 是 | 应用私钥，RSA2 签名 |
| `billing.alipay_public_key` | string | 是 | 支付宝公钥或证书公钥 |
| `billing.alipay_seller_id` | string | 否 | 可选，用于回调校验收款账号 |
| `billing.wechat_app_id` | string | 否 | 微信支付 AppID |
| `billing.wechat_mch_id` | string | 否 | 微信支付商户号 |
| `billing.wechat_api_v3_key` | string | 是 | 微信支付 API v3 密钥 |
| `billing.wechat_private_key` | string | 是 | 商户私钥 |
| `billing.wechat_serial_no` | string | 否 | 商户证书序列号 |
| `billing.wechat_platform_cert` | string | 是 | 微信支付平台证书或证书缓存引用 |
| `billing.epay_gateway_url` | string | 否 | 兼容易支付 `submit.php` 网关 |
| `billing.epay_pid` | string | 否 | 易支付商户 ID |
| `billing.epay_key` | string | 是 | 易支付商户密钥 |
| `billing.epay_types` | string | 否 | JSON 字符串，默认 `[{"name":"支付宝","type":"alipay"},{"name":"微信支付","type":"wxpay"}]` |

配置规则：

- `billing.payment_providers` 为空或 `disabled` 时，前端隐藏在线支付入口，只允许 SystemAdmin 手工开通或调额。
- 启用 `alipay` 时，必须同时配置 `alipay_app_id`、`alipay_private_key`、`alipay_public_key`；生产要求 `billing.public_api_base_url` 为 HTTPS。
- 启用 `wechat_pay` 时，必须同时配置 `wechat_app_id`、`wechat_mch_id`、`wechat_api_v3_key`、`wechat_private_key`、`wechat_serial_no`；生产要求 `billing.public_api_base_url` 为 HTTPS。
- 启用 `epay` 时，必须同时配置 `epay_gateway_url`、`epay_pid`、`epay_key`、`epay_types`。
- 支付密钥不能通过普通 settings 列表明文返回；现有 `system_settings.is_secret` 需要真正用于服务端脱敏和前端“留空表示不修改”的交互，生产环境建议再做数据库加密或只从环境变量/KMS 注入。

支付通知地址：

```text
POST /api/v1/billing/payments/alipay/notify
POST /api/v1/billing/payments/wechat/notify
GET  /api/v1/billing/payments/epay/notify
POST /api/v1/billing/payments/epay/notify
```

协议校验要点：

- 支付宝：下单使用 `alipay.trade.page.pay` 或 `alipay.trade.precreate`；异步通知校验 RSA2 签名、`app_id`、`out_trade_no`、`trade_status`、`total_amount` 和可选 `seller_id`，只有 `TRADE_SUCCESS` / `TRADE_FINISHED` 可以发放权益。
- 微信支付：V1 优先 `transactions/native` 二维码；异步通知校验 `Wechatpay-Signature`、`Wechatpay-Timestamp`、`Wechatpay-Nonce`、`Wechatpay-Serial`，用 API v3 密钥解密 `resource`，校验 `trade_state=SUCCESS`、`out_trade_no`、`amount.payer_total`、`mchid` 和 `appid`。
- EPay：沿用 DEEIX-Chat 的 `submit.php` 页面跳转和 MD5 签名模式，`type` 从 `epay_types` 中选择 `alipay` 或 `wxpay`。
- 所有渠道都必须以 `billing_payment_orders` 的金额、币种、订单号和状态为准；任何第三方扩展字段只写入快照，不作为前端可控权益。

### 7.5 购买商品表

套餐价格仍使用 `billing_prices`。一次性购买积分和存储包使用独立商品表，前端只提交 `item_id`，价格和权益以后端配置为准。

```sql
CREATE TABLE billing_purchase_items (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    code VARCHAR(64) NOT NULL UNIQUE,
    item_type VARCHAR(32) NOT NULL,
    edition_scope VARCHAR(32) NOT NULL DEFAULT 'all',
    name VARCHAR(128) NOT NULL,
    description VARCHAR(512) NOT NULL DEFAULT '',
    currency VARCHAR(16) NOT NULL DEFAULT 'CNY',
    amount_cents BIGINT NOT NULL DEFAULT 0,
    credit_point_micros BIGINT NOT NULL DEFAULT 0,
    storage_quota_bytes BIGINT NOT NULL DEFAULT 0,
    duration_days INT NOT NULL DEFAULT 0,
    status VARCHAR(32) NOT NULL DEFAULT 'active',
    sort_order INT NOT NULL DEFAULT 0,
    metadata_json JSONB NOT NULL DEFAULT '{}',
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_billing_purchase_items_type_status
    ON billing_purchase_items (item_type, edition_scope, status, sort_order);
```

商品类型：

```text
topup          购买积分
storage_addon 购买存储包
```

商品规则：

- `topup` 商品必须有 `credit_point_micros > 0`，且 `storage_quota_bytes=0`。
- `storage_addon` 商品必须有 `storage_quota_bytes > 0`，且 `credit_point_micros=0`。
- `edition_scope` 控制个人版、企业版或全部可买；企业版购买入口仅 Owner/Admin 可见和可用。
- `duration_days=0` 表示长期有效；非 0 时支付成功后写入 `tenant_storage_addon_grants.expires_at`。

V1 商品示例：

| code | 类型 | 金额 | 权益 | 适用 |
| --- | --- | ---: | ---: | --- |
| `credit_10000_points` | `topup` | 99 元 | 10,000 积分 | 个人版、企业版 |
| `credit_50000_points` | `topup` | 399 元 | 50,000 积分 | 企业版 |
| `storage_20gb` | `storage_addon` | 29 元 | 20 GB | 个人版、企业版 |
| `storage_100gb` | `storage_addon` | 99 元 | 100 GB | 企业版 |

### 7.6 支付订单表

支付订单参考 DEEIX-Chat 的 `PaymentOrder`：先创建内部订单，再创建第三方收银台；回调只根据内部 `order_no` 幂等发放权益。

```sql
CREATE TABLE billing_payment_orders (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    order_no VARCHAR(64) NOT NULL UNIQUE,
    order_type VARCHAR(32) NOT NULL,
    tenant_id BIGINT NOT NULL,
    actor_user_id VARCHAR(36) NOT NULL,
    plan_id VARCHAR(36),
    price_id VARCHAR(36),
    item_id VARCHAR(36),
    provider VARCHAR(32) NOT NULL DEFAULT '',
    payment_method VARCHAR(32) NOT NULL DEFAULT '',
    status VARCHAR(32) NOT NULL DEFAULT 'pending',
    currency VARCHAR(16) NOT NULL DEFAULT 'CNY',
    amount_cents BIGINT NOT NULL DEFAULT 0,
    credit_point_micros BIGINT NOT NULL DEFAULT 0,
    storage_quota_bytes BIGINT NOT NULL DEFAULT 0,
    billing_interval VARCHAR(16) NOT NULL DEFAULT '',
    cycles INT NOT NULL DEFAULT 1,
    external_payment_ref VARCHAR(128) NOT NULL DEFAULT '',
    external_checkout_ref VARCHAR(128) NOT NULL DEFAULT '',
    checkout_url TEXT NOT NULL DEFAULT '',
    qr_code_url TEXT NOT NULL DEFAULT '',
    notify_url TEXT NOT NULL DEFAULT '',
    return_url TEXT NOT NULL DEFAULT '',
    provider_payload_json JSONB NOT NULL DEFAULT '{}',
    notify_snapshot_json JSONB NOT NULL DEFAULT '{}',
    paid_at TIMESTAMP WITH TIME ZONE,
    expired_at TIMESTAMP WITH TIME ZONE,
    snapshot_json JSONB NOT NULL DEFAULT '{}',
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_billing_payment_orders_tenant_created
    ON billing_payment_orders (tenant_id, created_at DESC);

CREATE INDEX idx_billing_payment_orders_status_expired
    ON billing_payment_orders (status, expired_at);
```

订单类型：

```text
subscription
topup
storage_addon
manual_contract
```

订单状态：

```text
pending
paid
failed
expired
closed
reconciliation
```

支付渠道与方法：

```text
provider=alipay      payment_method=page|qr_code|wap
provider=wechat_pay  payment_method=native|jsapi|h5
provider=epay        payment_method=alipay|wxpay
```

支付完成事务规则：

1. 根据 `order_no` 锁定 `billing_payment_orders`。
2. `status=paid` 时直接返回成功，表示重复通知已处理。
3. `status` 不是 `pending` 或订单已过期时，进入 `reconciliation` 或拒绝通知。
4. 校验第三方通知金额、币种、商户号和订单号与订单快照一致。
5. `subscription`：替换或续期 `tenant_subscriptions`，应用套餐存储和周期积分。
6. `topup`：锁定 `tenant_credit_accounts`，增加 `credit_point_micros`，写 `tenant_credit_transactions(type=topup, ref_no=order_no)`。
7. `storage_addon`：创建 `tenant_storage_addon_grants(order_no)`，增加 `tenants.storage_quota`，写 `tenant_storage_transactions(type=storage_addon, ref_no=order_no)`。
8. 同一事务把订单更新为 `paid`，写 `paid_at`、`external_payment_ref` 和回调快照。

## 8. 企业分配用量

企业版总额度属于企业工作区，但成员实际能用多少必须通过分配规则决定。分配可以来自套餐默认模板，也可以由 Owner/Admin 单独调整。

```sql
CREATE TABLE tenant_member_credit_allocations (
    id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
    tenant_id BIGINT NOT NULL,
    user_id VARCHAR(36) NOT NULL,
    period_start_at TIMESTAMP WITH TIME ZONE NOT NULL,
    period_end_at TIMESTAMP WITH TIME ZONE NOT NULL,
    allocated_period_point_micros BIGINT NOT NULL DEFAULT 0,
    allocated_balance_point_micros BIGINT NOT NULL DEFAULT 0,
    status VARCHAR(32) NOT NULL DEFAULT 'active',
    created_by_user_id VARCHAR(36) NOT NULL DEFAULT '',
    updated_by_user_id VARCHAR(36) NOT NULL DEFAULT '',
    snapshot_json JSONB,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX uq_tenant_member_credit_allocations_period
    ON tenant_member_credit_allocations (tenant_id, user_id, period_start_at, period_end_at);
```

规则：

- `allocated_period_point_micros=0` 且 `allocated_balance_point_micros=0` 表示成员没有企业分配用量，不允许新增付费调用。
- 成员已用量从 `tenant_usage_ledgers` 按 `usage_scope='enterprise_allocated_usage'`、`tenant_id`、`actor_user_id` 和当前周期聚合，不依赖前端传值。
- `allocated_period_point_micros` 从企业周期额度中划拨，周期到期后失效。
- `allocated_balance_point_micros` 从企业余额中授权，适合临时增配或项目制额度。
- 企业总消耗仍受 `tenant_subscriptions.period_credit_point_micros` 和 `tenant_credit_accounts.balance_point_micros` 限制，成员分配不能让企业总账透支。
- Owner/Admin 可以查看企业总额、成员分配、成员已用和剩余额度。
- Contributor/Viewer 只能查看自己的企业分配用量，不查看企业总账细节，除非后续配置开放。

## 9. 核心流程

### 9.1 注册个人版

```text
用户注册
  -> 创建 users
  -> 自动创建 personal tenant
  -> 创建 owner tenant_member
  -> 创建 tenant_credit_account
  -> 创建 personal_free subscription
  -> 应用套餐 storage_quota
  -> 返回 /auth/me 的 edition + subscription + storage + credits
```

所有账号入口必须一致：

- 公开注册；
- 管理员创建账号；
- 邀请注册；
- OIDC 首次登录；
- 历史 tenantless 账号修复。

### 9.2 购买/升级个人版

```text
GET /billing/plans?edition=personal
  -> 选择价格
  -> 创建 subscription 支付单
  -> 支付回调幂等确认
  -> 替换当前 personal tenant 的 active subscription
  -> 写入周期积分和存储额度
  -> 返回最新 billing overview
```

约束：

- 只能为自己的个人工作区购买个人版套餐。
- 不能通过个人套餐打开企业成员、共享空间和企业发布能力。
- 升级立即生效；降级到期生效。

### 9.3 开通企业版

```text
个人 Owner 点击开通企业
  -> 创建 enterprise tenant
  -> 创建 owner tenant_member
  -> 创建 tenant_credit_account
  -> 创建 enterprise_trial 或 enterprise_team subscription
  -> 应用企业存储和可分配积分额度
  -> 为 Owner 创建默认 enterprise_allocated_usage
  -> 初始化默认存储、知识库默认配置
  -> 返回可切换工作区
```

不做的事：

- 不改变个人工作区订阅。
- 不迁移个人知识库。
- 不把个人知识库自动共享给企业。
- 不把员工个人积分并入企业。

### 9.4 企业续费/升级

企业 Owner/Admin 可发起订阅变更：

- 升级：立即应用新能力、新成员数、新存储和新周期积分。
- 降级：默认到当前周期结束后生效；如果强制立即降级，必须先校验成员数、存储、API Key、共享空间、Agent 数量等是否超限。
- 续费：延长 `current_period_end_at` 或创建下一段连续订阅。
- 取消自动续费：设置 `cancel_at_period_end=true`，到期变为 `expired` 或回到免费/试用策略。

企业员工不能变更企业订阅，除非其角色是 Owner/Admin 且套餐能力允许。

### 9.5 支付下单通用流程

```text
用户选择购买积分或存储包
  -> 前端读取 /billing/payment-config 和可购买商品
  -> 提交 item_id + payment_provider + payment_method
  -> 后端按 active tenant 判断购买主体
  -> 校验套餐状态、角色权限、商品 edition_scope 和支付配置
  -> 创建 billing_payment_orders(status=pending)
  -> 调用支付宝/微信支付/EPay 创建收银台
  -> 保存 checkout_url / qr_code_url / provider_payload_json
  -> 前端跳转收银台或展示二维码
  -> 支付平台异步 notify
  -> 校验签名、商户、金额、币种、订单号和状态
  -> 同一事务标记 paid 并发放积分或存储
  -> 前端轮询订单状态并刷新 billing overview
```

购买主体规则：

- 个人工作区：本人可以为自己的个人工作区购买积分和允许的存储包。
- 企业工作区：仅 Owner/Admin 可以为企业工作区购买积分、存储包和订阅；普通成员只能查看自己被分配的企业用量。
- 前端不能传入金额、积分数或存储字节数作为权威值，只能传 `item_id`；后端从 `billing_purchase_items` 或 `billing_prices` 生成订单快照。
- 支付成功权益发放只认异步 notify；前端 return URL 只能展示“支付结果确认中”，不能直接发放。

### 9.6 购买积分

```text
创建 topup 订单
  -> 选择支付宝或微信支付
  -> 支付成功
  -> 锁定 tenant_credit_account
  -> balance_point_micros += order.credit_point_micros
  -> 写 tenant_credit_transactions(type=topup)
  -> 返回新余额
```

充值积分属于当前订阅主体：

- 在个人工作区充值，只能个人使用；
- 在企业工作区充值，进入企业余额账户，必须分配给成员后才能使用；
- 不在个人和企业之间自动转移。

### 9.7 购买存储包

```text
创建 storage_addon 订单
  -> 选择支付宝或微信支付
  -> 支付成功
  -> 锁定 tenant 行
  -> 创建 tenant_storage_addon_grants(order_no 唯一)
  -> tenants.storage_quota += order.storage_quota_bytes
  -> 写 tenant_storage_transactions(type=storage_addon)
  -> 返回新存储额度
```

购买存储包的约束：

- 个人版是否允许购买存储包由商品 `edition_scope` 和产品策略控制。
- 企业版购买存储包只增加企业工作区存储，不增加员工个人工作区存储。
- 重复支付通知必须命中同一个 `order_no`，不能重复加存储。
- 如果购买前已处于 `storage_over_quota`，支付成功后重新计算状态；仍超额则继续阻断新增写入。

### 9.8 AI 调用扣积分

```text
解析 active tenant
  -> 校验套餐状态和功能
  -> 解析 usage_scope: personal_usage | enterprise_allocated_usage
  -> 企业场景解析成员 allocation_id 和剩余额度
  -> 解析模型/服务定价
  -> 估算本次积分
  -> 创建 tenant_usage_reservation
  -> 调用模型/服务
  -> 计算真实积分
  -> 写 tenant_usage_ledger
  -> 按用量口径扣周期额度或余额额度
  -> 释放/结算 reservation
```

失败处理：

- 用户取消且上游未产生费用：释放预留，不写扣费账本。
- 上游返回可计费用量但前端断连：继续结算账本。
- 上游费用已产生但账本写入失败：reservation 进入 `reconciliation`，后台补偿。
- 幂等重试必须复用 `ref_no`，不能重复扣积分。

### 9.9 知识库上传扣存储与积分

知识库上传同时涉及存储和积分：

```text
校验 KB 写权限
  -> 校验套餐允许知识库上传
  -> 预留存储
  -> 保存原始文件
  -> 创建 knowledge
  -> 解析/切片/向量化
  -> 对 OCR/ASR/Embedding/摘要/图谱等计积分
  -> 结算 storage_used
  -> 写 usage_ledger 和 storage_transaction
```

重要边界：

- 存储不足阻断上传或重解析。
- 积分不足可以阻断需要 AI 的处理步骤；若原始文件已经上传，状态应明确为 `failed` 或 `pending_payment`，不能伪装成解析失败。
- 纯文本入库如果不调用付费模型，可以只计存储不扣积分。

## 10. 后端服务设计

### 10.1 BillingPolicyService

```go
type BillingPolicyService interface {
    GetBillingOverview(ctx context.Context, tenantID uint64) (*BillingOverview, error)
    ResolveEntitlements(ctx context.Context, tenantID uint64) (*EffectiveEntitlements, error)
    RequireFeature(ctx context.Context, tenantID uint64, feature string) error
    CheckLimit(ctx context.Context, tenantID uint64, limit string, current int64) error
}
```

职责：

- 读取当前工作区订阅；
- 合并版本、套餐、覆盖配置；
- 返回前端可展示的能力和限制；
- 给后端业务服务做统一能力校验。

### 10.2 SubscriptionService

```go
type SubscriptionService interface {
    ListPlans(ctx context.Context, edition types.EditionCode) ([]*BillingPlan, error)
    GetTenantSubscription(ctx context.Context, tenantID uint64) (*TenantSubscription, error)
    SetTenantSubscriptionByAdmin(ctx context.Context, input AdminSubscriptionInput) error
    CancelAtPeriodEnd(ctx context.Context, tenantID uint64, actorUserID string) error
}
```

职责：

- 套餐展示；
- 订阅创建、续费、升级、取消；
- 免费/试用套餐初始化；
- 套餐生效时同步存储额度和周期积分。

### 10.3 PaymentService

```go
type PaymentService interface {
    ListPaymentProviders(ctx context.Context) (*PaymentProviderConfig, error)
    ListPurchaseItems(ctx context.Context, tenantID uint64, itemType string) ([]*PurchaseItem, error)
    CreateCheckout(ctx context.Context, input CheckoutInput) (*PaymentOrder, error)
    GetPaymentOrder(ctx context.Context, tenantID uint64, orderNo string) (*PaymentOrder, error)
    HandleAlipayNotify(ctx context.Context, values url.Values) error
    HandleWechatPayNotify(ctx context.Context, headers http.Header, body []byte) error
    HandleEPayNotify(ctx context.Context, values url.Values) error
    CloseExpiredOrders(ctx context.Context, now time.Time) error
}
```

职责：

- 读取支付渠道配置；
- 创建内部支付订单；
- 调用支付宝、微信支付或 EPay 收银台；
- 校验异步通知签名、金额、币种、商户和订单状态；
- 在同一事务内幂等发放订阅、积分或存储包；
- 记录支付审计和待核对状态。

### 10.4 CreditService

```go
type CreditService interface {
    GetAccount(ctx context.Context, tenantID uint64) (*TenantCreditAccount, error)
    AddBalance(ctx context.Context, input CreditGrantInput) error
    ResolveUsageScope(ctx context.Context, tenantID uint64, actorUserID string) (*UsageScope, error)
    ListMemberAllocations(ctx context.Context, tenantID uint64, period BillingPeriod) ([]*MemberCreditAllocation, error)
    UpsertMemberAllocation(ctx context.Context, input MemberCreditAllocationInput) error
    AuthorizeUsage(ctx context.Context, input UsageEstimateInput) (*UsageAuthorization, error)
    EnsureBudget(ctx context.Context, authorization *UsageAuthorization, requiredPointMicros int64) error
    SettleUsage(ctx context.Context, authorization *UsageAuthorization, usage UsageLedgerInput) error
    ReleaseUsage(ctx context.Context, authorization *UsageAuthorization) error
    MarkReconciliation(ctx context.Context, authorization *UsageAuthorization, reason string) error
}
```

职责：

- 积分余额；
- 周期额度；
- 个人用量和企业分配用量解析；
- 企业成员分配额度；
- 调用前预留；
- 真实用量结算；
- 余额流水；
- 并发和幂等。

### 10.5 StorageQuotaService

```go
type StorageQuotaService interface {
    GetUsage(ctx context.Context, tenantID uint64) (*StorageUsage, error)
    Reserve(ctx context.Context, input StorageReserveInput) (*StorageReservation, error)
    Settle(ctx context.Context, reservationID string, actualBytes int64, ref StorageRef) error
    Release(ctx context.Context, reservationID string, reason string) error
    ApplyPlanQuota(ctx context.Context, tenantID uint64, quotaBytes int64, ref SubscriptionRef) error
    AdminSetQuota(ctx context.Context, tenantID uint64, quotaBytes int64, actorUserID string) error
}
```

职责：

- 统一所有写入链路的存储校验；
- 取代零散的 `tenant.StorageUsed >= tenant.StorageQuota` 判断；
- 记录存储流水；
- 支持订阅变更、存储包和后台手工调整。

## 11. API 设计

### 11.1 用户侧接口

除 `my-usage` 外，用户侧接口按当前 active tenant 解析订阅主体；`my-usage` 同时返回当前账号的个人用量和当前企业工作区分配用量。

```text
GET  /api/v1/billing/plans?edition=personal|enterprise
GET  /api/v1/billing/payment-config
GET  /api/v1/billing/purchase-items?type=topup|storage_addon
GET  /api/v1/billing/overview
GET  /api/v1/billing/my-usage
GET  /api/v1/billing/usage?scope=personal_usage|enterprise_allocated_usage
GET  /api/v1/billing/usage/daily
GET  /api/v1/billing/credits/transactions
GET  /api/v1/billing/storage
POST /api/v1/billing/subscriptions/checkout
POST /api/v1/billing/topups/checkout
POST /api/v1/billing/storage-addons/checkout
GET  /api/v1/billing/orders/:order_no
POST /api/v1/billing/subscriptions/cancel-at-period-end
```

`GET /billing/payment-config` 返回：

```json
{
  "currency": "CNY",
  "providers": [
    {
      "provider": "alipay",
      "name": "支付宝",
      "methods": ["page", "qr_code"]
    },
    {
      "provider": "wechat_pay",
      "name": "微信支付",
      "methods": ["native"]
    }
  ]
}
```

`POST /billing/topups/checkout` 请求：

```json
{
  "item_id": "credit_10000_points",
  "payment_provider": "alipay",
  "payment_method": "page",
  "success_url": "https://app.example.com/platform/billing?payment=success",
  "cancel_url": "https://app.example.com/platform/billing?payment=cancel"
}
```

`POST /billing/storage-addons/checkout` 请求：

```json
{
  "item_id": "storage_20gb",
  "payment_provider": "wechat_pay",
  "payment_method": "native"
}
```

`checkout` 响应：

```json
{
  "order_no": "PAY202609140001",
  "order_type": "topup",
  "provider": "alipay",
  "payment_method": "page",
  "status": "pending",
  "amount_cents": 9900,
  "currency": "CNY",
  "checkout_url": "https://openapi.alipay.com/...",
  "qr_code_url": "",
  "expired_at": "2026-09-14T12:30:00Z"
}
```

`GET /billing/overview` 返回：

```json
{
  "tenant_id": 10001,
  "edition": "enterprise",
  "subscription": {
    "plan_code": "enterprise_team",
    "status": "active",
    "period_start_at": "2026-09-01T00:00:00Z",
    "period_end_at": "2026-10-01T00:00:00Z",
    "cancel_at_period_end": false
  },
  "usage_scope": "enterprise_allocated_usage",
  "personal_usage": {
    "tenant_id": 90001,
    "period_total_points": 20000,
    "period_used_points": 6000,
    "period_remaining_points": 14000,
    "balance_points": 1000
  },
  "enterprise_allocated_usage": {
    "tenant_id": 10001,
    "tenant_name": "睿乐示例园区",
    "allocation_id": "alloc_202609_user01",
    "allocated_period_points": 10000,
    "period_used_points": 2500,
    "period_remaining_points": 7500,
    "allocated_balance_points": 2000,
    "balance_used_points": 0,
    "balance_remaining_points": 2000
  },
  "credits": {
    "tenant_period_total_points": 100000,
    "tenant_period_used_points": 25000,
    "tenant_period_remaining_points": 75000,
    "tenant_balance_points": 5000,
    "visible_to_current_user": true
  },
  "storage": {
    "quota_bytes": 107374182400,
    "used_bytes": 32212254720,
    "remaining_bytes": 75161927680,
    "status": "ok"
  },
  "limits": {
    "tenant.max_members": 10,
    "tenant.max_api_keys": 3
  },
  "features": {
    "workspace.members.manage": true,
    "billing.payment_online": true,
    "billing.topup": true,
    "billing.storage_addon_purchase": true
  }
}
```

### 11.2 企业管理员接口

```text
GET  /api/v1/tenants/:id/billing/overview
GET  /api/v1/tenants/:id/billing/usage/by-member
GET  /api/v1/tenants/:id/billing/usage/by-agent
GET  /api/v1/tenants/:id/billing/usage/by-knowledge-base
GET  /api/v1/tenants/:id/billing/allocations
PUT  /api/v1/tenants/:id/billing/allocations/:user_id
POST /api/v1/tenants/:id/billing/allocations/bulk
GET  /api/v1/tenants/:id/billing/invoices
GET  /api/v1/tenants/:id/billing/payment-orders
```

权限：

- `Viewer+` 可看自己的用量；
- `Admin+` 可看企业总览、成员消耗、企业分配用量和订单；
- `Owner` 或 SystemAdmin 才能执行订阅降级、取消和合同信息变更；
- API Key 默认不能访问账单，除非显式增加 `manage_billing` capability。

### 11.3 SystemAdmin 接口

挂在现有 `/api/v1/system/admin/*` 下：

```text
GET    /api/v1/system/admin/billing/plans
POST   /api/v1/system/admin/billing/plans
PUT    /api/v1/system/admin/billing/plans/:id
GET    /api/v1/system/admin/billing/payment-settings
PUT    /api/v1/system/admin/billing/payment-settings
GET    /api/v1/system/admin/billing/purchase-items
POST   /api/v1/system/admin/billing/purchase-items
PUT    /api/v1/system/admin/billing/purchase-items/:id
DELETE /api/v1/system/admin/billing/purchase-items/:id
GET    /api/v1/system/admin/billing/prices
POST   /api/v1/system/admin/billing/prices
PUT    /api/v1/system/admin/billing/prices/:id
GET    /api/v1/system/admin/billing/tenants
PUT    /api/v1/system/admin/billing/tenants/:id/subscription
PUT    /api/v1/system/admin/billing/tenants/:id/credits
PUT    /api/v1/system/admin/billing/tenants/:id/storage-quota
GET    /api/v1/system/admin/billing/payment-orders
POST   /api/v1/system/admin/billing/payment-orders/:order_no/reconcile
GET    /api/v1/system/admin/billing/usage-ledgers
GET    /api/v1/system/admin/billing/reconciliation
POST   /api/v1/system/admin/billing/reconciliation/:id/settle
GET    /api/v1/system/admin/billing/model-prices
PUT    /api/v1/system/admin/billing/model-prices
GET    /api/v1/system/admin/billing/service-prices
PUT    /api/v1/system/admin/billing/service-prices
```

现有 `/api/v1/system/admin/tenants/:id/storage-quota` 可保留，但最终应由 `StorageQuotaService.AdminSetQuota` 复用同一条存储流水。

## 12. 前端与后台页面

### 12.1 主前端

个人版显示：

- 当前套餐；
- 个人用量：周期积分、余额积分和用量明细；
- 存储用量；
- 升级个人版；
- 购买个人积分；
- 购买个人存储包，是否展示由产品策略控制；
- 开通企业版；
- 隐藏企业成员、企业存储实例、共享空间、API Key、发布渠道等入口。

企业版显示：

- 企业套餐；
- 我的企业分配用量；
- 企业总周期积分和余额积分，限 Owner/Admin 可见；
- 存储用量；
- 成员用量排行；
- 成员分配用量；
- Owner/Admin 可升级、续费、购买企业积分、购买企业存储包；
- 普通成员只显示自己的企业分配用量和用量明细；
- 企业账单和发票入口。

### 12.2 Admin 工程

Admin 工程分两类入口：

| 入口 | 使用人 | 范围 |
| --- | --- | --- |
| 企业工作区管理 | 企业 Owner/Admin | 当前企业工作区订阅、用量、成员分配、存储 |
| 平台运营管理 | SystemAdmin | 全部套餐、价格、支付配置、购买商品、订阅、手工调额、对账、模型定价 |

导航建议：

```text
工作区
  ├─ 成员管理
  ├─ 订阅与用量
  ├─ 存储配额
  └─ 审计日志

平台运营
  ├─ 套餐与价格
  ├─ 支付配置
  ├─ 积分与存储商品
  ├─ 工作区订阅
  ├─ 积分与充值
  ├─ 模型/服务定价
  ├─ 用量账本
  └─ 待核对账单
```

页面状态：

- `active`：正常；
- `trialing`：显示试用剩余时间；
- `payment_pending`：订单已创建，等待支付平台异步通知；
- `past_due`：限制新增消耗，提示续费；
- `storage_over_quota`：允许删除和读取，禁止新增存储；
- `credits_exhausted`：禁止付费 AI 调用，允许充值；
- `suspended`：只允许 Owner/Admin 和 SystemAdmin 处理账单。

## 13. 后端接入点

### 13.1 鉴权顺序

所有会产生权益或消耗的请求按此顺序：

```text
JWT / API Key 认证
  -> 解析 active tenant
  -> 验证 tenant_members / API Key principal
  -> 解析 subscription + entitlements
  -> 解析 usage_scope 和企业成员 allocation
  -> 校验 feature / limit / quota
  -> 资源级权限校验
  -> 预留积分或存储
  -> 执行业务
  -> 结算账本
```

注意：

- 资源级权限不能被订阅绕过。
- 订阅状态不能被前端请求体覆盖。
- API Key 的扣费主体是 key 所属工作区，账本 `actor_user_id` 可记录 synthetic principal 或创建人。

### 13.2 知识库链路

需要接入：

- 创建知识库；
- 文件上传；
- URL 导入；
- FAQ 导入；
- 手工知识发布；
- 重解析；
- 知识图谱提取；
- 摘要和问题生成；
- 删除和批量删除释放存储；
- 复制/移动知识时重新校验目标工作区存储和积分。

### 13.3 会话/Agent 链路

需要接入：

- 普通问答；
- Agent 问答；
- 临时文件问答；
- IM/Embed 渠道问答；
- 原生工具调用；
- MCP 工具调用；
- Web Search；
- 模型调试接口。

每次调用都应产生同一类 `tenant_usage_ledger`，方便企业总览和成员排行复用。

### 13.4 支付链路

支付回调路由必须挂在公开 API 下，不要求用户登录，但不能绕过业务校验：

```text
公开 notify
  -> 限制请求体大小和回调频率
  -> 读取支付渠道配置
  -> 验签和解密
  -> 校验商户、金额、币种、订单号和支付状态
  -> 锁定 billing_payment_orders
  -> 幂等发放订阅、积分或存储
  -> 写审计和 notify_snapshot_json
  -> 返回支付平台要求的成功响应
```

支付链路必须保证：

- 第三方回调不能携带或覆盖 `tenant_id`、`actor_user_id`、`credit_point_micros`、`storage_quota_bytes`。
- 支付宝/微信的同步返回 URL 只用于展示，不作为发放依据。
- 订单过期后即使收到迟到回调，也进入 `reconciliation`，由 SystemAdmin 人工核对。
- 支付平台的订单号、流水号、交易状态和原始金额写入 `notify_snapshot_json`，用于后续对账。

## 14. 数据迁移

### 14.1 迁移顺序

1. 新增套餐、价格、订阅、购买商品、支付订单、积分账户、积分流水、用量预留、用量账本、存储预留、存储流水、存储包发放表。
2. 为所有现有 `tenants` 创建 `tenant_credit_accounts`。
3. 根据 `space_type` 或现有分类，为工作区创建默认免费订阅：
   - `personal` -> `personal_free`
   - `organization` -> `enterprise_team` 或 `enterprise_trial`
   - 未分类 -> `legacy` 兼容订阅，后台待分类
4. 将现有 `tenants.storage_quota` 作为迁移初始额度，不强行覆盖。
5. 从 `knowledges.storage_size` 汇总校验 `tenants.storage_used`，只输出差异报告；生产环境不自动修正。
6. 上线后新注册、新建企业工作区必须立即创建订阅和积分账户。
7. 等后台分类完成后，再把关键企业能力切到强制订阅校验。

### 14.2 不允许的迁移

- 不修改 `knowledge_bases.tenant_id`。
- 不移动或复制对象存储文件。
- 不重写向量库和资源注册路径。
- 不将员工个人工作区合并到企业。
- 不把知识库快捷订阅迁移成套餐订阅。
- 不删除超出新套餐额度的历史文件。

### 14.3 兼容策略

- 没有订阅记录的工作区，后端创建默认免费/legacy 订阅后再返回能力。
- 订阅服务异常时，涉及付费或企业能力的写操作默认拒绝；读操作可以按历史权限降级展示。
- `storage_quota <= 0` 目前有“不限制”的语义，订阅上线后只允许 SystemAdmin 设置，普通工作区套餐不得使用无限额。

## 15. 分阶段实施

每阶段独立提交、独立验证，验收通过后再进入下一阶段。

### Phase 0：设计落地准备

交付：

- 本设计文档评审；
- 确认套餐名称、额度、价格；
- 确认积分定价口径；
- 确认支付宝、微信支付商户主体、证书模式、支付方式和回调域名。

验收：

- 产品确认个人版/企业版边界；
- 技术确认订阅主体为 `tenant_id`；
- 运维确认 SystemAdmin 后台职责。

### Phase 1：订阅基础模型

交付：

- 迁移表；
- 类型和仓储；
- 默认套餐 seed；
- 工作区默认订阅和积分账户；
- `/billing/overview`。

验收：

- 新旧工作区都能返回订阅概览；
- 个人和企业工作区套餐不同；
- 未订阅工作区不会导致前端崩溃。

### Phase 2：存储配额统一

交付：

- `StorageQuotaService`；
- 存储预留和存储流水；
- 知识库上传、解析、删除、FAQ 导入接入；
- 后台存储配额页面复用新服务。

验收：

- 并发上传不会超过配额；
- 删除知识能释放存储；
- 降级超额只阻断新增，不影响读取和删除。

### Phase 3：积分计费闭环

交付：

- 模型/服务定价；
- `CreditService`；
- 用量预留、结算、释放、待核对；
- 个人用量和企业分配用量解析；
- 会话、Agent、知识库处理接入；
- 用量账本列表。

验收：

- 个人工作区只消耗个人用量；
- 企业工作区只消耗成员企业分配用量；
- 同一用量口径内周期额度优先消耗；
- 超出后扣对应余额额度；
- 余额不足时付费调用被拒绝；
- 幂等重试不重复扣费；
- 上游已产生费用但结算失败时进入待核对。

### Phase 4：订阅变更和支付

交付：

- 套餐列表；
- 订阅升级/续费/取消；
- 支付宝和微信支付配置；
- 积分和存储包商品配置；
- 购买积分；
- 购买存储包；
- 支付订单和回调幂等；
- 订单状态查询和过期关闭；
- 手工合同开通。

验收：

- 免费、试用、月付、年付可配置；
- 用户可以用支付宝或微信支付创建积分订单并入账；
- 用户可以用支付宝或微信支付创建存储包订单并增加工作区存储额度；
- 支付成功后订阅、积分或存储按订单类型生效；
- 重复回调不重复发放积分或存储；
- 金额、币种、商户或签名不匹配的回调不会发放权益；
- 取消自动续费到期后正确降级。

### Phase 5：企业治理和后台

交付：

- 企业成员用量；
- 企业成员分配用量；
- 企业账单导出；
- SystemAdmin 套餐、定价、订阅、对账页面；
- 审计事件和告警。

验收：

- Owner/Admin 能看到企业总用量和成员排行；
- Owner/Admin 能调整成员企业分配用量；
- 普通成员只能看自己的企业分配用量；
- SystemAdmin 能处理待核对账单；
- 后台调额全量写审计。

## 16. 测试清单

后端测试：

- 个人工作区默认创建 `personal_free` 订阅；
- 企业工作区默认创建企业订阅；
- 个人版企业能力全部后端拒绝；
- 企业 Owner/Admin/Contributor/Viewer 的订阅管理权限矩阵；
- 套餐升级、降级、续费、取消状态机；
- 支付宝配置缺失时 checkout 被拒绝；
- 微信支付配置缺失时 checkout 被拒绝；
- 支付回调签名、金额、币种、商户号校验；
- `topup` 支付成功只增加当前订阅主体积分；
- `storage_addon` 支付成功只增加当前订阅主体存储；
- 重复支付回调不重复发放积分或存储；
- 个人用量和企业分配用量互不串用；
- 企业成员未分配额度时付费调用被拒绝；
- 周期积分、余额积分、超额扣费顺序；
- 并发调用积分预留；
- 并发上传存储预留；
- 删除知识释放存储；
- 重试、断连、上游成功但本地结算失败的待核对；
- API Key 扣费主体和权限不越界。

前端测试：

- `/auth/me` 和 `/billing/overview` 更新后菜单状态一致；
- 个人版不显示企业入口；
- 企业版按角色显示订阅、个人用量、企业分配用量、存储和成员分配；
- 个人和企业购买页只展示当前工作区可购买商品；
- 支付中、支付成功、支付失败、订单过期状态可被正确展示；
- 超额/余额不足/待续费状态文案明确；
- 直接访问隐藏路由仍被后端拒绝；
- 移动端存储和积分卡片不溢出。

数据测试：

- 旧工作区 backfill 后不改资源归属；
- `tenants.storage_used` 与 `knowledges.storage_size` 差异只报告不自动修；
- 订阅订单重复回调幂等；
- 购买商品快照和订单金额在回调后可追溯；
- 用量账本按日、成员、模型、知识库聚合准确；
- 用量账本按 `usage_scope` 和 `allocation_id` 聚合准确。

## 17. 风险与决策点

需要产品确认：

- 个人版是否允许付费购买额外存储包。
- 企业版周期积分是否按成员数阶梯增加，以及是否自动提高默认成员分配额度。
- 企业成员默认分配用量是按角色、固定值还是由 Owner 首次配置。
- 积分是否有过期时间。
- 免费版是否允许使用高成本模型。
- 支付宝使用电脑网页支付、扫码支付还是两者都支持。
- 微信支付 V1 使用 Native 扫码还是也支持 JSAPI/H5。
- EPay 是否作为兼容通道保留，还是只做支付宝和微信支付原生接入。

需要技术确认：

- 当前所有 AI 调用链路的统一计费切入点。
- 向量索引存储估算是否足够准确，是否需要定期 reconciliation。
- 临时文件、导出文件、聊天附件是否纳入工作区存储。
- API Key 账本中的 `actor_user_id` 使用创建人还是 synthetic principal。
- `storage_quota <= 0` 的无限额语义是否继续保留。
- 支付密钥是否需要在 `system_settings` 层做数据库加密，还是全部由环境变量/KMS 注入。
- 微信支付平台证书的下载、缓存和轮换策略。

主要风险：

- 如果只做前端套餐展示，后端仍会越权或超额使用。
- 如果订阅绑定用户，企业多人协作会出现扣费主体混乱。
- 如果没有预留机制，并发调用和上传会透支积分/存储。
- 如果企业成员没有强制分配口径，会退化成共享池并导致个人/企业用量混账。
- 如果支付配置只做前端校验，错误商户号或回调域名会导致订单长期 pending。
- 如果回调未校验金额和商户，可能出现小额支付发放大额权益。
- 如果没有账本快照，后续调价会导致历史对账不可追溯。
- 如果把知识库订阅误当付费订阅，会混淆权限和套餐权益。

## 18. 非目标

本设计不包含：

- 发票和税务系统的具体实现；
- 退款、分账和代理商结算；
- 多级集团版/总部版；
- 个人用量和企业分配用量互转；
- 自动删除超额文件；
- 个人知识库自动迁移到企业；
- 两套独立部署包；
- 前端绕过后端直接修改套餐能力。

## 19. 参考文件

Ruile 当前工程：

- `internal/types/tenant.go`
- `internal/types/edition.go`
- `internal/handler/dto/tenant.go`
- `internal/types/knowledge_subscription.go`
- `internal/application/service/knowledge_subscription.go`
- `internal/application/service/knowledge_create.go`
- `internal/application/service/knowledge_process.go`
- `internal/application/repository/tenant.go`
- `internal/router/router.go`
- `internal/types/system_setting.go`
- `internal/application/service/system_setting.go`
- `docs/RBAC说明.md`
- `docs/共享空间说明.md`
- `docs/业务架构说明.md`

DEEIX-Chat 参考：

- `backend/internal/infra/persistence/models/billing.go`
- `backend/internal/domain/billing/types.go`
- `backend/internal/application/billing/service.go`
- `backend/internal/application/billing/payment_checkout.go`
- `backend/internal/infra/persistence/postgres/billing/repository.go`
- `backend/internal/infra/persistence/models/chat.go`
- `backend/internal/infra/persistence/postgres/conversation/repository.go`
- `backend/internal/transport/http/billing/router.go`
- `backend/internal/transport/http/billing/payment.go`
- `backend/internal/infra/payment/epay/client.go`
- `backend/internal/ports/payment/checkout.go`
- `backend/internal/application/settings/registry.go`
- `backend/internal/application/settings/validators.go`
- GitHub 项目：[https://github.com/DEEIX-AI/DEEIX-Chat](https://github.com/DEEIX-AI/DEEIX-Chat)

支付官方参考：

- 支付宝异步通知说明：[https://opendocs.alipay.com/open/203/105286](https://opendocs.alipay.com/open/203/105286)
- 微信支付 API v3 Go SDK 和回调验签解密：[https://github.com/wechatpay-apiv3/wechatpay-go](https://github.com/wechatpay-apiv3/wechatpay-go)
